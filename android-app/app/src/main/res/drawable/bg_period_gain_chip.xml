<?xml version="1.0" encoding="utf-8"?>
<!-- Solid color is a placeholder - MainActivity.bindPeriodGainLine sets
     the real color per chip at runtime via GradientDrawable.setColor,
     since each of the 3 chips needs an independent gain/loss/neutral
     tint, not one shared drawable. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners android:radius="10dp" />
    <solid android:color="@color/colorNeutralBg" />
</shape>
